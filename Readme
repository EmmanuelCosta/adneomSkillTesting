### Readme #####
################

# The project is a maven project
# This is a utility project which provides helpfull function
# It can be added has a dependencies to be used by other program
# In order to push that librairie into your maven repository consider run a `mvn clean install`